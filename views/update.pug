h2 Update sách

if dataDetail
ul
 each x in dataDetail
  li Tên sách:
   span(style="font-weight: 600; color: #333; margin-left: 5px") #{x.title}
  li Mô tả:
   span(style="color: #333; margin-left: 5px") #{x.description}
  h3 Edit
  form(action="/books/update/" + x.id, method="POST")
    label Tên sách 
    input(type="text", name="titleUpdate")
    br
    label Mô tả sách
    input(type="text", name="descriptionUpdate")
    br
    button submit edit


a(href="/books") Go back home